app:
  mariaDB4j:
    databaseName: wallet
mariaDB4j:
  dataDir:
  port: 3307
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/wallet
    username: root
    password:
    driverClassName: org.mariadb.jdbc.Driver
    testWhileIdle: true
    validationQuery: SELECT 1
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect